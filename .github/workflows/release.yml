name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (e.g., 1.0.0)'
        required: true
        type: string
      build_windows:
        description: '构建 Windows x64'
        required: true
        type: boolean
        default: true
      build_linux:
        description: '构建 Linux x64'
        required: true
        type: boolean
        default: true
      create_release:
        description: '是否创建 GitHub Release'
        required: true
        type: boolean
        default: false
  push:
    tags:
      - 'v*'

env:
  APP_NAME: DiabloEdit

jobs:
  build-windows:
    name: Build Windows x64
    if: github.event_name == 'push' || github.event.inputs.build_windows == 'true'
    runs-on: windows-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install Qt6
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.*'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_msvc2022_64'

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Deploy Qt DLLs
        run: windeployqt build/Release/${{ env.APP_NAME }}.exe --release --no-compiler-runtime

      - name: Package
        shell: pwsh
        run: |
          $version = "${{ github.event.inputs.version || github.ref_name }}"
          $version = $version -replace '^v', ''
          Copy-Item -Recurse data build/Release/data
          Copy-Item -Recurse resources build/Release/resources
          Compress-Archive -Path build/Release/* -DestinationPath ${{ env.APP_NAME }}-windows-x64-$version.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-windows-x64
          path: ${{ env.APP_NAME }}-windows-x64-*.zip

  build-linux:
    name: Build Linux x64
    if: github.event_name == 'push' || github.event.inputs.build_linux == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install system dependencies & Qt6
        run: |
          sudo apt-get update -y
          sudo apt-get install -y qt6-base-dev libglx-dev libgl1-mesa-dev

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Package
        run: |
          version="${{ github.event.inputs.version || github.ref_name }}"
          version="${version#v}"
          cp -r data build/
          cp -r resources build/
          cd build
          tar czf ../${{ env.APP_NAME }}-linux-x64-${version}.tar.gz *
          cd ..

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-linux-x64
          path: ${{ env.APP_NAME }}-linux-x64-*.tar.gz

  create-release:
    name: Create Release
    needs: [build-windows, build-linux]
    if: github.event_name == 'push' || github.event.inputs.create_release == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Download Windows artifact
        if: github.event_name == 'push' || github.event.inputs.build_windows == 'true'
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-windows-x64

      - name: Download Linux artifact
        if: github.event_name == 'push' || github.event.inputs.build_linux == 'true'
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-linux-x64

      - name: Determine tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            version="${{ github.event.inputs.version }}"
            [[ "$version" != v* ]] && version="v${version}"
            echo "tag=${version}" >> $GITHUB_OUTPUT
          else
            echo "tag=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: DiabloEdit ${{ steps.tag.outputs.tag }}
          files: |
            *.zip
            *.tar.gz
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
